<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/onboarding_bg">

    <TextView
        android:id="@+id/calibrationHint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_margin="24dp"
        android:gravity="center"
        android:lineSpacingExtra="4dp"
        android:text="@string/calibrate_notch_body"
        android:textColor="@color/gray_text"
        android:textSize="14sp" />

    <Button
        android:id="@+id/btnSaveNotch"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_marginStart="24dp"
        android:layout_marginEnd="24dp"
        android:layout_marginBottom="80dp"
        android:text="@string/btn_save_notch_position" />

    <View
        android:id="@+id/draggablePill"
        android:layout_width="@dimen/calibration_pill_width"
        android:layout_height="@dimen/calibration_pill_height"
        android:background="@drawable/calibration_pill_border" />

</FrameLayout>
